<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/single_dialog_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/public_dialog_bg"
    android:orientation="vertical">

    <TextView
        android:id="@+id/single_dialog_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="12dp"
        android:layout_marginTop="28dp"
        android:text="@string/str_tips"
        android:textColor="@android:color/black"
        android:textSize="15sp"/>

    <TextView
        android:id="@+id/single_dialog_content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/ll_btn_container"
        android:layout_below="@+id/single_dialog_title"
        android:gravity="center"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        android:paddingLeft="15dp"
        android:paddingRight="15dp"
        tools:text="content"
        android:textColor="#777777"
        android:textSize="14sp"/>

    <LinearLayout
        android:id="@+id/ll_btn_container"
        android:orientation="vertical"
        android:layout_alignParentBottom="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <View
            android:visibility="gone"
            android:id="@+id/view_btn_top_line"
            android:background="#E6E6E6"
            android:layout_marginLeft="1dp"
            android:layout_marginRight="1dp"
            android:layout_width="match_parent"
            android:layout_height="0.5dp"/>

        <TextView
            android:layout_marginBottom="1.5dp"
            android:layout_marginRight="1dp"
            android:layout_marginLeft="1dp"
            android:id="@+id/single_dialog_button"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:background="@drawable/public_dialog_btn_selector"
            android:gravity="center"
            android:text="@string/str_sure"
            android:textColor="@color/public_colorAccent"
            android:textSize="15sp" />

    </LinearLayout>
</RelativeLayout>